services:
  django:
    build: .
    container_name: django

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: redis

    ports:
      - "6379:6379"